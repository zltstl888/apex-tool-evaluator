# Evaluation fixtures

`evals.json` contains normal, edge, and negative behavior fixtures for manual or external agent evaluation.

Continuous integration validates the file's structure and required expectations; it does not run a language model or prove behavioral performance. Model, runtime, prompt, date, sampling, and run count can change behavioral results, so any published comparison must report those conditions and avoid generalizing from a single run.
